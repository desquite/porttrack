packages:
  - "apps/*"
  - "packages/*"

# Packages whose postinstall build scripts we accept to run.
# Add to this list deliberately — pnpm prompts before executing.
allowBuilds:
  '@parcel/watcher': true
  '@swc/core': true
  esbuild: true
  sharp: true
  unrs-resolver: true
